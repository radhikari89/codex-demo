# Services

Backend services live in this directory so the repository can grow into a multi-service architecture over time.

- `userservice`: Spring Boot REST API starter service with PostgreSQL and Swagger/OpenAPI support.
